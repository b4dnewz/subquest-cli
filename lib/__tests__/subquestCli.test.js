const assert = require('assert');
// Const subquestCli = require('../index.js');

describe('subquestCli', () => {
  it('has a test', () => {
    assert(true, 'subquestCli should have a test');
  });
});
